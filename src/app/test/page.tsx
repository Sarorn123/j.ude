"use client";

import React from "react";
import CustomUploadThing from "../components/reusable/custom-upload-thing";

type Props = {};

const Page = (props: Props) => {
  return <CustomUploadThing />;
};

export default Page;
